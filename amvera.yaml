meta:
  environment: node
  toolchain:
    name: npm
    version: "20"

build:
  additionalCommands: npm install && npm run build
  artifacts:
    /source/dist: /app

run:
  command: npx serve -s /app -l 80
  containerPort: 80
  persistenceMount: /data
